# Phase 12 run 20260818T074454Z-adff4a51f8b8e118 — maintainer confirmation

**Source:** maintainer-supplied local file (Chinese AI-product-management Q&A
session) — local origin, intaken via `vcp plan <file>` (maintainer's own
material; no third-party host), 34m58s.
**Content hash:** `sha256:f10e8895e2370c2f4bbbe98218f699d7ef17582c538604e23fbf6b1698a48889`
(intake bytes; 129,909,345 B, h264 720p + aac, **no embedded subtitle stream**).
**Run identity:** `20260818T074454Z-adff4a51f8b8e118`; RunBundle
`outputs/f10e8895…a48889/20260818T074454Z-adff4a51f8b8e118/`; `vcp verify`
(hash_verified + inventory_valid) and `vcp inventory` **pass**.
**Download plan:** n/a — local-file material (still hashed at intake). Plan
`8cb2d4c4d953982cdd608028`, confirmed with front-loaded run choices
`asr_mode=full_asr`, `visual_text_enabled=false`.
**Formal branch(es) claimed:** branch **2 — full ASR** (no usable subtitle track;
speech transcribed end-to-end). Probed and actual agree: the source carries no
subtitle stream, so the subtitle stage handed off to ASR and the whole timeline
was transcribed.

Reviewed and decided: **PENDING maintainer review** (per-capability D10 ratings
below are not yet recorded; this run is not counted toward branch coverage until
the maintainer records them).

## Per-capability confirmation (D10)

Verbal rating per capability: **acceptable** / **marginal** / **unacceptable**.
**Left for the maintainer** — an agent never records the D10 confirmation
(ledger closure protocol; `AGENTS.md`).

| Capability | Rating | Notes |
|---|---|---|
| subtitle readability | _pending_ | ASR produced 74 cue-bound subtitles spanning 0.0s–34.9min (full duration). |
| speaker separation | _n/a this run_ | Diarization is off run #1's full-ASR critical path (not promoted); no speaker labels produced. |
| summary faithfulness | _pending_ | text_semantics produced 1 verified SemanticSegment ("AI时代下的职业危机与流量生存策略"). Thin — see the truncation leftover below. |

Overall decision: **PENDING maintainer confirmation.**

## Notes and follow-ups

- **Pause/resume drill (D-run requirement): exercised.** The run was paused with
  `vcp pause` at the transcription→text_analysis stage boundary (run parked
  `paused`, nothing published), then continued with `vcp resume`; the resumed run
  adopted the completed source/subtitle/audio/transcription units (their report
  ids rebuilt from the run state) and ran only text_analysis to a published
  terminal. No completed work was recomputed.
- **text_semantics truncation leftover (measured, not assumed).** On this
  35-minute source the transcript is windowed into overlapping technical blocks
  so each model call fits the KV window; within a block the model reliably emits
  the JSON envelope (the whole-transcript single call had degraded to free prose).
  Some verbose blocks still truncate at `max_tokens` (4096), so only one segment
  survived adjudication — the analysis is **thin** (1 segment for a 35-minute
  multi-topic talk). This is a block-size / `max_tokens` calibration tradeoff for
  maintainer tuning (ADR 0056), recorded here as the run #1 finding. The D7
  single-segment-collapse leftover therefore manifests on real long material.
- **Real-media defects found and fixed during run #1** (all committed this
  session): analysis-audio derivation now reads the true PCM frame count
  (real audio is not sample-aligned); preprocessing profile corrected 48 kHz→16 kHz
  (`v2`); per-capability peak envelopes recalibrated to full-length values;
  forced_alignment + diarization de-promoted (off the full-ASR path); orchestrated
  full-ASR resource confirmation granted up front (a decision pause is terminal);
  the stage report-id chain is rebuilt on resume; the real text_semantics bridge
  is reached and accepts the full-ASR subtitle handoff; text_semantics windows
  long sources into technical blocks and strips Markdown code fences.
- **Timing.** Wall clock ≈ 4950 s (~82 min), dominated by ASR (~43 min) and
  text_semantics (~24 min) running under host memory pressure (16 GiB machine,
  near-full; macOS memory compression active). This is an environment
  characteristic, not a pipeline fault.
- **CER/WER:** no proofed human reference text exists for this source, so no
  CER/WER is recorded (credential-free quality only).

## Provenance (model stack that produced these outputs)

From the RunBundle processing report:

| Model | Revision | sha256 | Purpose |
|---|---|---|---|
| mlx-community/Qwen3-ASR-1.7B-8bit | `a8379a2e2f9e313c9292cdf1af4055ab56d50d55` | `aa010c938dbd1722034a0d0bb315f92202488bfbfe6ef4b48f3f5207b8e299ee` | Primary ASR (zh+en mixed speech) |
| mlx-community/Qwen3-4B-Instruct-2507-8bit | `0e42af58449718de7931ee04f28191fbe6c43a56` | `2a1b7ba81037c15882e233f7a1e40dd41251d51fb4afac48a164a3cc61e479e2` | Text semantics (zh+en summaries + JSON) |
| snakers4/silero-vad (silero_vad.onnx) | `v6.2.1` | `0898f9226ecd53bbc80e54b9511c9047ae66c9e5ec13c7dc822195e7fc1dc1d6` | Voice-activity detection + shared ≤5-min chunking upstream |

- **Tools:** FFmpeg 9.0.1 (analysis-audio derivation), FFprobe (structure/coverage).
- **Environment:** Apple Silicon (arm64), MLX GPU backend; Python 3.12; fully
  offline (`HF_HUB_OFFLINE`; no network, no automatic downloads).
- **Resources (observed, from subprocess evidence):** peak memory **5.36 GiB**
  (run peak; per capability — VAD 396 MiB, ASR 3.37 GiB, text_semantics 5.36 GiB),
  well within the 12 GiB envelope; wall clock ≈ 4950 s; disk delta ~5 KB (the
  RunBundle documents; source, ASR, and derivatives are retained under `work/`).
