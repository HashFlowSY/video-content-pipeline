# Phase 12 run 20260818T114653Z-19562f62a649ee1b — maintainer confirmation

**Source:** maintainer-supplied local file (Chinese AI-product-management Q&A
session) — local origin, intaken via `vcp plan <file>` (maintainer's own
material; no third-party host), 34m58s.
**Content hash:** `sha256:f10e8895e2370c2f4bbbe98218f699d7ef17582c538604e23fbf6b1698a48889`
(intake bytes; 129,909,345 B, h264 720p + aac, **no embedded subtitle stream**).
**Run identity:** `20260818T114653Z-19562f62a649ee1b`; RunBundle
`outputs/f10e8895…a48889/20260818T114653Z-19562f62a649ee1b/`; `vcp verify`
(hash_verified + inventory_valid) and `vcp inventory` **pass**.
**Download plan:** n/a — local-file material (still hashed at intake). Plan
`8cb2d4c4d953982cdd608028`, confirmed with front-loaded run choices
`asr_mode=full_asr`, `visual_text_enabled=false`.
**Formal branch(es) claimed:** branch **2 — full ASR** (no usable subtitle track;
speech transcribed end-to-end). Probed and actual agree: the source carries no
subtitle stream, so the subtitle stage handed off to ASR and the whole timeline
was transcribed.

> Supersedes two earlier attempts on this source, both complete + verified:
> `20260818T074454Z-adff4a51f8b8e118` (text_semantics ran the pre-fix engine and
> produced no validated segment content) and `20260818T111753Z-45c7c50cac559ecf`
> (corrected text engine, but its published `content-report.md` rendered only the
> segment title). This run uses the corrected text engine (single whole-transcript
> call, token-efficient cue ids, `detailed_content` field) *and* the corrected
> content-report renderer, so its published `content-report.md` carries the segment
> title plus all 74 cue-cited detail points.

Reviewed and decided: **PENDING maintainer review** (per-capability D10 ratings
below are not yet recorded; this run is not counted toward branch coverage until
the maintainer records them).

## Per-capability confirmation (D10)

Verbal rating per capability: **acceptable** / **marginal** / **unacceptable**.
**Left for the maintainer** — an agent never records the D10 confirmation
(ledger closure protocol; `AGENTS.md`).

| Capability | Rating | Notes |
|---|---|---|
| subtitle readability | _pending_ | ASR produced 74 cue-bound subtitles spanning 0.0s–34.9min (full duration). |
| speaker separation | _n/a this run_ | Diarization is off run #1's full-ASR critical path (not promoted); no speaker labels produced. |
| summary faithfulness | _pending_ | text_semantics produced 1 adjudicated SemanticSegment ("AI产品经理的职业危机与自媒体的长期价值") with **74 cue-cited detail points** (unsupported_item_count=0). Content-rich, but a single broad segment — see the granularity note below. |

Overall decision: **PENDING maintainer confirmation.**

## Notes and follow-ups

- **Pause/resume drill (D-run requirement): exercised.** Paused with `vcp pause`
  at the transcription→text_analysis stage boundary (run parked `paused`, nothing
  published), then continued with `vcp resume`; the resumed run rebuilt the
  completed source/subtitle/audio/transcription report-id chain from run state and
  ran only text_analysis to a published terminal. No completed work was recomputed.
- **text_semantics on real long material — now content-rich.** The whole
  34m58s transcript is analysed in a single model call (Qwen3-4B natively supports
  a 262144-token context; the earlier overflow was caused only by a conservative
  max_kv_size=8192 and a prompt that repeated the 64-hex source id on every cue
  line). The current engine raises the window to 32768, renders token-efficient
  Part-local cue aliases (`P{index}:{position}`) remapped back to full canonical
  ids before adjudication, and emits detail items under the field the validator
  reads (`detailed_content`). Result: one adjudicated segment covering all 74 cues
  with a real Chinese title and 74 cue-grounded detail points, all within the
  envelope.
- **Granularity leftover (measured, for maintainer tuning).** The model grouped
  the whole multi-topic talk into a **single** semantic segment (≈ one detail per
  cue) rather than splitting it into sub-topics. This is a model/prompt
  quality-tuning matter (ADR 0056): the calibration is now
  `qualification_scope: pending_reconfirmation` and awaits the maintainer's
  real-sample quality re-confirmation (and any multi-segment prompt tuning). The
  D7 single-segment-collapse observation therefore still applies on real long
  material, but the segment now carries validated content rather than being empty.
- **Pre-flight context-budget gate.** Before loading the model the engine counts
  the rendered prompt's real tokens and, if `prompt + max_tokens` would exceed
  `max_kv_size`, stops with `text_semantics_context_budget_exceeded` for the
  maintainer to decide whether to raise the window (confirming peak stays within
  12 GiB). This run fit and proceeded.
- **Timing.** Wall clock dominated by ASR (~43 min) and text_semantics (~12 min)
  under host memory pressure (16 GiB machine, near-full; macOS memory compression
  active) — an environment characteristic, not a pipeline fault.
- **CER/WER:** no proofed human reference text exists for this source, so none is
  recorded (credential-free quality only).

## Provenance (model stack that produced these outputs)

From the RunBundle processing report:

| Model | Revision | sha256 | Purpose |
|---|---|---|---|
| mlx-community/Qwen3-ASR-1.7B-8bit | `a8379a2e2f9e313c9292cdf1af4055ab56d50d55` | `aa010c938dbd1722034a0d0bb315f92202488bfbfe6ef4b48f3f5207b8e299ee` | Primary ASR (zh+en mixed speech) |
| mlx-community/Qwen3-4B-Instruct-2507-8bit | `0e42af58449718de7931ee04f28191fbe6c43a56` | `2a1b7ba81037c15882e233f7a1e40dd41251d51fb4afac48a164a3cc61e479e2` | Text semantics (zh summary segment + cue-cited details) |
| snakers4/silero-vad (silero_vad.onnx) | `v6.2.1` | `0898f9226ecd53bbc80e54b9511c9047ae66c9e5ec13c7dc822195e7fc1dc1d6` | Voice-activity detection + shared ≤5-min chunking upstream |

- **Tools:** FFmpeg 9.0.1 (analysis-audio derivation), FFprobe (structure/coverage).
- **Environment:** Apple Silicon (arm64), MLX GPU backend; Python 3.12; fully
  offline (`HF_HUB_OFFLINE`; no network, no automatic downloads).
- **Resources (observed, from subprocess evidence):** peak memory **5.87 GiB**
  (run peak; per capability — VAD 396 MiB, ASR 3.37 GiB, text_semantics 5.87 GiB),
  well within the 12 GiB envelope (the text_semantics candidate's 11 GiB estimate
  is the ceiling for a source that fills the 32768-token window; this source used
  ~24k tokens). Wall clock ≈ 55 min under memory pressure; disk delta small (the
  RunBundle documents; source, ASR, and derivatives are retained under `work/`).
